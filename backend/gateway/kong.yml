_format_version: "2.1"
_transform: true

services:
  - name: users-service
    url: http://users-service:8000
    routes:
      - name: users-route
        paths:
          - /api/users
        strip_path: true

  # - name: orders-service
  #   url: http://orders-service:8000
  #   routes:
  #     - name: orders-route
  #       paths:
  #         - /api/orders
  #       strip_path: true

  # - name: user-orders-route
  #   url: http://orders-service:8000
  #   routes:
  #     - name: user-orders-route
  #       paths:
  #         - /api/users/*/orders
  #       strip_path: false

  # Refer this section add your service (personal Fast API)
  # - name: disease-service
  #   url: http://host.docker.internal:5050 # run your application in a selected port.
  #   routes:
  #     - name: disease-route
  #       paths:
  #         - /predict # define a base path in your API
  #       strip_path: false

  - name: disease-service
    url: http://host.docker.internal:8000 # run your application in a selected port.
    routes:
      - name: disease-route
        paths:
          - /predict # define a base path in your API
        strip_path: false
